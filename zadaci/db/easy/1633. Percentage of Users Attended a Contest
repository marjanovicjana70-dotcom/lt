select contest_id,round(count(*)*100.00/(select count(*) from users),2)as percentage
from register
group by contest_id
order by round(count(*)*100.00/(select count(*) from users),2) desc,contest_id asc
