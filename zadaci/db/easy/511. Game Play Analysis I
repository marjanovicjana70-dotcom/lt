with nesto as ( select player_id,event_date, rank() over(PARTITION by player_id order by event_date asc) as rankiranje
from activity)
select player_id, event_date as first_login
from nesto
where rankiranje = 1
